export const H2 = ({ heading, className }) => {
    return <h2 className={`font-bold text-lightBlack text-4xl1 leading-110 ${className}`}>{heading} <span className={`after:absolute relative after:h-[5px] after:w-[64px] after:bottom-[-20px] ${lineclassname}`}>{linetext}</span></h2>
}